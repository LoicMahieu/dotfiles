# Keep aliases minimal — git aliases live in gitconfig.shared

alias ll='ls -lah'
alias gs='git status -sb'
alias gd='git diff'
alias gfa='git fetch --all --tags --prune --jobs=10'
alias gp='git push'
